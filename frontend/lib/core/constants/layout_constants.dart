library;

class LayoutConstants {
  const LayoutConstants._();

  static const double sidebarWidth = 280.0;
  static const double sidebarCollapsedWidth = 72.0;
  static const double mobileBreakpoint = 800.0;
  static const double gridBreakpoint = 700.0;
  static const int sprintDurationDays = 14;
  static const double avatarSize = 64.0;
  static const double iconSizeSm = 14.0;
  static const double iconSizeMd = 20.0;
  static const double iconSizeLg = 24.0;
}

class AnimationConstants {
  const AnimationConstants._();

  static const Duration hoverDuration = Duration(milliseconds: 200);
  static const double hoverScale = 1.005;
  static const Duration transitionDuration = Duration(milliseconds: 300);
}
