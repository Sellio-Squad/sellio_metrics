name: 🧑‍💻 SWE-agent

# Session Checkpoint Architecture:
# ─────────────────────────────────
# When the agent fails mid-task (rate limits, crash), instead of committing
# incomplete code, we save a structured "session state" as a GitHub Actions
# artifact. On the next run, the artifact is downloaded and injected into the
# task prompt so the agent skips exploration and resumes from its plan.
#
# Session state lives at /tmp/ai_session.md (written by the agent itself).
# Artifact name: session-{owner}-{repo}-{issue_number}
# Retention: 7 days (enough for retries, auto-cleaned after).

on:
  workflow_dispatch:
    inputs:
      owner:
        description: "Target repo owner (e.g. Sellio-Squad)"
        required: true
        type: string
      repo:
        description: "Target repo name (e.g. sellio_mobile)"
        required: true
        type: string
      issue_number:
        description: "Issue number"
        required: true
        type: string
      task_id:
        description: "Unique task ID from Cloudflare KV"
        required: true
        type: string
      branch_name:
        description: "Branch to create and push to (e.g. ai/42-fix-login)"
        required: true
        type: string

jobs:
  swe-agent:
    name: "🧑‍💻 SWE-agent Issue #${{ inputs.issue_number }} in ${{ inputs.repo }}"
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      # ── 1. Generate GitHub App installation token ──────────────────────
      - name: "Generate App token"
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: ${{ inputs.owner }}

      # ── 2. Checkout sellio_metrics (scripts) ───────────────────────────
      - name: "📦 Checkout sellio_metrics"
        uses: actions/checkout@v4
        with:
          path: sellio_metrics

      # ── 3. Checkout the TARGET repo ────────────────────────────────────
      - name: "📦 Checkout ${{ inputs.owner }}/${{ inputs.repo }}"
        uses: actions/checkout@v4
        with:
          repository: "${{ inputs.owner }}/${{ inputs.repo }}"
          token: ${{ steps.app-token.outputs.token }}
          path: target_repo
          ref: develop
          fetch-depth: 0

      # ── 4. Fetch issue details from GitHub API ─────────────────────────
      - name: "📋 Fetch issue details"
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh api /repos/${{ inputs.owner }}/${{ inputs.repo }}/issues/${{ inputs.issue_number }} \
            --jq '.title' > /tmp/issue_title.txt
          gh api /repos/${{ inputs.owner }}/${{ inputs.repo }}/issues/${{ inputs.issue_number }} \
            --jq '.body'  > /tmp/issue_body.txt
          echo "📋 Issue: $(cat /tmp/issue_title.txt)"

      # ── 5. Auto-detect stack ───────────────────────────────────────────
      - name: "🔍 Detect stack"
        id: detect
        run: |
          cd target_repo
          if find . -name "pubspec.yaml" -not -path "*/build/*" | grep -q .; then
            echo "stack=flutter" >> $GITHUB_OUTPUT
            echo "Detected: Flutter/Dart"
          elif find . -name "build.gradle*" | grep -q .; then
            echo "stack=kotlin" >> $GITHUB_OUTPUT
            echo "Detected: Kotlin/Spring Boot"
          elif [ -f "package.json" ]; then
            echo "stack=node" >> $GITHUB_OUTPUT
            echo "Detected: Node/TypeScript"
          else
            echo "stack=unknown" >> $GITHUB_OUTPUT
            echo "Unknown stack"
          fi

      # ── 5a. Setup Flutter ──────────────────────────────────────────────
      - name: "Setup Flutter"
        if: steps.detect.outputs.stack == 'flutter'
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.x"
          channel: "stable"
          cache: true

      # ── 5b. Setup Java 17 (Kotlin/Spring Boot) ────────────────────────
      - name: "Setup Java 17"
        if: steps.detect.outputs.stack == 'kotlin'
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: gradle

      # ── 5c. Setup Node 22 ─────────────────────────────────────────────
      - name: "Setup Node 22"
        if: steps.detect.outputs.stack == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: target_repo/package-lock.json

      # ── 6. Setup Python & install mini-swe-agent ──────────────────────
      - name: "🐍 Setup Python 3.11"
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: "📦 Install mini-swe-agent"
        run: pip install mini-swe-agent

      # ── 7. Configure git ───────────────────────────────────────────────
      - name: "Configure git"
        run: |
          cd target_repo
          git config user.email "sellio-bot@sellio.app"
          git config user.name "Sellio Bot"
          git checkout -b "${{ inputs.branch_name }}"

      # ── 8. Download session state from previous run (if any) ──────────
      # The session state file is a structured markdown document written by
      # the agent itself. It records: exploration findings, implementation
      # plan, completed steps, remaining steps, and known pitfalls.
      # No incomplete code is ever committed — only the session plan is saved.
      - name: "📥 Restore session state (if previous run exists)"
        id: session
        uses: actions/download-artifact@v4
        with:
          name: "session-${{ inputs.owner }}-${{ inputs.repo }}-${{ inputs.issue_number }}"
          path: /tmp/session
        continue-on-error: true   # silently skip if no prior session

      - name: "📋 Check session state"
        id: session_check
        run: |
          SESSION_FILE="/tmp/session/ai_session.md"
          if [ -f "$SESSION_FILE" ]; then
            SIZE=$(wc -c < "$SESSION_FILE")
            echo "is_resume=true" >> $GITHUB_OUTPUT
            echo "✅ Found session state from a previous run (${SIZE} bytes)"
            echo "── Session preview ──"
            head -60 "$SESSION_FILE"
          else
            echo "is_resume=false" >> $GITHUB_OUTPUT
            echo "✨ No prior session found. Starting fresh."
          fi

      # ── 9. Build task description (stack-aware + session-aware) ───────
      - name: "Build task description"
        env:
          STACK:        ${{ steps.detect.outputs.stack }}
          IS_RESUME:    ${{ steps.session_check.outputs.is_resume }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          OWNER:        ${{ inputs.owner }}
          REPO:         ${{ inputs.repo }}
          WORKSPACE:    ${{ github.workspace }}/target_repo
        run: |
          python3 << 'PYEOF'
          import os
          from pathlib import Path

          stack        = os.environ.get("STACK", "unknown")
          is_resume    = os.environ.get("IS_RESUME", "false") == "true"
          issue_number = os.environ.get("ISSUE_NUMBER", "?")
          owner        = os.environ.get("OWNER", "")
          repo         = os.environ.get("REPO", "")
          workspace    = os.environ.get("WORKSPACE", "/workspace")
          issue_title  = Path("/tmp/issue_title.txt").read_text().strip()
          issue_body   = Path("/tmp/issue_body.txt").read_text().strip()

          # ── Stack-specific build/verify steps ─────────────────────────
          if stack == "flutter":
              lang = "Flutter/Dart"
              build_steps = """
          4. Find ALL Flutter app directories:
               find . -name "pubspec.yaml" -not -path "*/build/*" -not -path "*/.dart_tool/*"
          5. For the relevant Flutter app, run: flutter pub get
          6. Analyze ONLY the files you created or modified (NEVER the whole project):
               dart analyze --fatal-warnings path/to/your/file.dart
             Fix EVERY error AND warning. Remove unused imports.
          7. Run: flutter test --no-pub --reporter compact
             Fix EVERY failure. Repeat until both analyze and tests pass."""
          elif stack == "kotlin":
              lang = "Kotlin/Spring Boot"
              build_steps = """
          4. Run: ./gradlew compileKotlin
          5. Fix EVERY compile error.
          6. Run: ./gradlew test
          7. Fix EVERY test failure. Repeat until clean."""
          elif stack == "node":
              lang = "Node.js/TypeScript"
              build_steps = """
          4. Run: npm run build
          5. Fix EVERY build error.
          6. Run: npm test
          7. Fix EVERY test failure. Repeat until clean."""
          else:
              lang = "the project language"
              build_steps = """
          4. Run the project's build command and fix any errors."""

          # ── Session state instructions ────────────────────────────────
          # The agent is instructed to maintain /tmp/ai_session.md so that
          # if this run is interrupted, the next run can resume from the plan.
          session_instructions = """
          SESSION STATE — MANDATORY:
          You MUST maintain a session state file at /tmp/ai_session.md.
          Update it at each major step so another AI can resume if you are interrupted.

          The file MUST follow this exact structure:
          ```
          # AI Session — Issue #<number>: <title>
          **Status:** IN PROGRESS | COMPLETE
          **Stack:** <detected stack>
          **Last Updated:** <timestamp from: date -u +%Y-%m-%dT%H:%M:%SZ>

          ## Exploration Findings
          - Key files discovered with their full paths and purpose
          - Navigation/routing patterns used
          - Class names and method signatures you verified exist

          ## Implementation Plan
          - [ ] Step 1: description
          - [x] Step 2: description (checked when done)
          - [ ] Step 3: description

          ## Completed Work
          Describe what changes were made and to which files.

          ## Remaining Work
          What still needs to be done to fully complete the issue.

          ## Known Pitfalls (Do NOT repeat these mistakes)
          - e.g. "flutter analyze on whole project generates 300 warnings — use dart analyze <file> instead"
          - Any other gotchas discovered during exploration
          ```

          Write the initial session state BEFORE writing any code.
          Update it AFTER completing each step.
          Mark **Status: COMPLETE** only when all tests pass and the issue is fully implemented."""

          # ── Resume context (injected when picking up a previous session)
          resume_context = ""
          session_file = Path("/tmp/session/ai_session.md")
          if is_resume and session_file.exists():
              prior = session_file.read_text().strip()
              resume_context = f"""
          ═══════════════════════════════════════════════════════════════
          RESUMING FROM PREVIOUS SESSION
          ═══════════════════════════════════════════════════════════════
          A previous AI agent worked on this issue but was interrupted.
          The session state it saved is below.

          READ IT CAREFULLY:
          - Skip any steps already marked [x] — they are DONE.
          - Start from the first unchecked [ ] step in the Implementation Plan.
          - Do NOT re-explore files already listed in "Exploration Findings".
          - Do NOT repeat mistakes listed in "Known Pitfalls".

          {prior}
          ═══════════════════════════════════════════════════════════════
          """

          # ── Full task prompt ──────────────────────────────────────────
          task = f"""You are implementing GitHub Issue #{issue_number} in a {lang} repository.

          Repository: {owner}/{repo}
          Issue #{issue_number}: {issue_title}

          Requirements:
          {issue_body}

          Your working directory is: {workspace}
          {resume_context}
          MANDATORY WORKFLOW — execute in this exact order:

          1. EXPLORE: List the project structure to understand WHERE the code lives.
          2. RESEARCH: Grep for classes/methods before writing code. Use only real verified APIs.
          3. Write the /tmp/ai_session.md session file with your exploration findings and plan.
          4. WRITE: Create or modify files using ONLY verified APIs.
          {build_steps}
          8. Update /tmp/ai_session.md: mark completed steps [x], update status to COMPLETE.
          9. When all checks pass, summarize what was done.
          {session_instructions}

          CRITICAL RULES:
          - NEVER invent class names, import paths, or constructor parameters.
          - Remove every unused import you introduce. Unused imports fail the workflow.
          - NEVER assume a file exists without checking with 'ls' or 'find'.
          - If the build/analyze fails, read the error carefully and fix it before retrying.
          - NEVER run 'flutter analyze' on the whole project. Only analyze files you touched.
          - Keep command output SHORT. Use 'head -50' or '--reporter compact'.
          - Focus ONLY on the issue. Do NOT fix pre-existing bugs in other files.
          """

          Path("/tmp/task.txt").write_text(task)
          mode = "RESUME" if is_resume else "FRESH"
          print(f"✅ Task written ({len(task)} bytes) — Mode: {mode}")
          PYEOF

      # ── 10. Run mini-swe-agent ─────────────────────────────────────────
      - name: "🤖 Run mini-swe-agent"
        id: agent
        working-directory: target_repo
        env:
          MSWEA_CONFIGURED: "true"
          # Claude is primary: best at code, separate quota from Gemini.
          # Falls back to Gemini Flash if ANTHROPIC_API_KEY is not set.
          # IMPORTANT: litellm requires the 'anthropic/' prefix to identify the provider.
          MSWEA_MODEL_NAME: ${{ secrets.ANTHROPIC_API_KEY != '' && 'anthropic/claude-haiku-4-5-20251001' || 'gemini/gemini-2.5-flash' }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GEMINI_API_KEY:   ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY_2: ${{ secrets.GEMINI_API_KEY_2 }}
          GEMINI_API_KEY_3: ${{ secrets.GEMINI_API_KEY_3 }}
          GEMINI_API_KEY_4: ${{ secrets.GEMINI_API_KEY_4 }}
          GEMINI_API_KEY_5: ${{ secrets.GEMINI_API_KEY_5 }}
          CI: "true"
        run: |
          mkdir -p ~/.config/mini-swe-agent
          echo "MSWEA_CONFIGURED=true" > ~/.config/mini-swe-agent/.env
          if [ -n "$ANTHROPIC_API_KEY" ]; then
            echo "MSWEA_MODEL_NAME=anthropic/claude-haiku-4-5-20251001" >> ~/.config/mini-swe-agent/.env
            echo "✅ Using Anthropic Claude as primary model"
          else
            echo "MSWEA_MODEL_NAME=gemini/gemini-2.5-flash" >> ~/.config/mini-swe-agent/.env
            echo "⚠️  ANTHROPIC_API_KEY not set — falling back to Gemini"
          fi
          echo "ANALYTICS_ENABLED=false" >> ~/.config/mini-swe-agent/.env

          python ../sellio_metrics/.github/scripts/run_mini_swe.py \
            --model "$MSWEA_MODEL_NAME" \
            --yolo \
            --task "$(cat /tmp/task.txt)"

      # ── 11. Save session state (always — success AND failure) ─────────
      # The agent writes /tmp/ai_session.md during its run.
      # We upload it as an artifact so the next run can resume from the plan.
      # NO incomplete code is committed to the repo.
      - name: "💾 Save session state"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: "session-${{ inputs.owner }}-${{ inputs.repo }}-${{ inputs.issue_number }}"
          path: /tmp/ai_session.md
          retention-days: 7
          overwrite: true
          if-no-files-found: ignore   # silently skip if agent crashed before writing

      # ── 12. Final verification (only runs if agent succeeded) ──────────
      - name: "✅ Final verification — Flutter"
        if: steps.agent.outcome == 'success' && steps.detect.outputs.stack == 'flutter'
        run: |
          find target_repo -name "pubspec.yaml" \
            -not -path "*/build/*" \
            -not -path "*/.dart_tool/*" \
            | xargs -I{} dirname {} \
            | while read APP_DIR; do
                echo "── Verifying: $APP_DIR ──"
                (cd "$APP_DIR" && flutter pub get) || { echo "pub get failed: $APP_DIR"; exit 1; }
                (cd "$APP_DIR" && flutter analyze --no-fatal-warnings) || { echo "analyze ERRORS in: $APP_DIR"; exit 1; }
                (cd "$APP_DIR" && flutter test --no-pub) || true
              done

      - name: "✅ Final verification — Kotlin"
        if: steps.agent.outcome == 'success' && steps.detect.outputs.stack == 'kotlin'
        run: cd target_repo && ./gradlew compileKotlin test

      - name: "✅ Final verification — Node"
        if: steps.agent.outcome == 'success' && steps.detect.outputs.stack == 'node'
        run: cd target_repo && npm run build && npm test

      # ── 13. Push branch (only on clean success) ────────────────────────
      - name: "🚀 Push branch to ${{ inputs.repo }}"
        if: success()
        run: |
          cd target_repo
          git add -A
          git diff --staged --stat
          git commit --allow-empty -m "feat(ai): implement #${{ inputs.issue_number }} [swe-agent]"
          git push origin "${{ inputs.branch_name }}"

      # ── 14. Notify Cloudflare — SUCCESS ───────────────────────────────
      - name: "Notify Cloudflare (success)"
        if: success()
        run: |
          if [ -z "${{ secrets.SELLIO_WORKER_URL }}" ]; then
            echo "❌ SELLIO_WORKER_URL secret is not set!"
            exit 1
          fi
          PAYLOAD=$(jq -n \
            --arg taskId    "${{ inputs.task_id }}" \
            --arg branch    "${{ inputs.branch_name }}" \
            --arg status    "success" \
            --arg owner     "${{ inputs.owner }}" \
            --arg repo      "${{ inputs.repo }}" \
            --argjson issueNum  "${{ inputs.issue_number }}" \
            '{taskId: $taskId, branch: $branch, status: $status, owner: $owner, repo: $repo, issueNumber: $issueNum}')

          HTTP_CODE=$(curl -s -o /tmp/cf_response.json -w "%{http_code}" --retry 3 -X POST \
            "${{ secrets.SELLIO_WORKER_URL }}/api/ai-pipeline/result" \
            -H "Content-Type: application/json" \
            -H "X-Sellio-Signature: ${{ secrets.SELLIO_WEBHOOK_SECRET }}" \
            -d "$PAYLOAD")
          echo "📡 Cloudflare [HTTP $HTTP_CODE]:"
          cat /tmp/cf_response.json || true
          [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ] || exit 1

      # ── 15. Notify Cloudflare — FAILURE ───────────────────────────────
      - name: "Notify Cloudflare (failure)"
        if: failure()
        run: |
          if [ -z "${{ secrets.SELLIO_WORKER_URL }}" ]; then
            echo "❌ SELLIO_WORKER_URL secret is not set!"
            exit 0
          fi
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          PAYLOAD=$(jq -n \
            --arg taskId   "${{ inputs.task_id }}" \
            --arg branch   "${{ inputs.branch_name }}" \
            --arg status   "failed" \
            --arg owner    "${{ inputs.owner }}" \
            --arg repo     "${{ inputs.repo }}" \
            --argjson issueNum "${{ inputs.issue_number }}" \
            --arg error    "SWE-agent failed. Session state saved. Logs: $RUN_URL" \
            '{taskId: $taskId, branch: $branch, status: $status, owner: $owner, repo: $repo, issueNumber: $issueNum, error: $error}')

          HTTP_CODE=$(curl -s -o /tmp/cf_response.json -w "%{http_code}" --retry 3 -X POST \
            "${{ secrets.SELLIO_WORKER_URL }}/api/ai-pipeline/result" \
            -H "Content-Type: application/json" \
            -H "X-Sellio-Signature: ${{ secrets.SELLIO_WEBHOOK_SECRET }}" \
            -d "$PAYLOAD")
          echo "📡 Cloudflare [HTTP $HTTP_CODE]:"
          cat /tmp/cf_response.json || true
